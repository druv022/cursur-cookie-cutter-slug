---
description: Re-vendor agent skills from upstream GitHub repos per scripts/skills-manifest.json
---

Run `./scripts/update-skills.sh` from the project root. Review `git diff` for skill changes. Read `SKILLS_LOCK.json` for pinned SHAs. Do not commit unless the user asks.
